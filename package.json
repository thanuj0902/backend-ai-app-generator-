{
  "name": "app-generator-platform",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/api",
    "packages/runtime-deployment"
  ],
  "scripts": {
    "dev:api": "npm run dev -w packages/api",
    "dev:runtime": "npm run dev -w packages/runtime-deployment",
    "build:api": "npm run build -w packages/api",
    "build:runtime": "npm run build -w packages/runtime-deployment",
    "db:generate": "npm run db:generate -w packages/api && npm run db:generate -w packages/runtime-deployment",
    "db:push": "npm run db:push -w packages/api && npm run db:push -w packages/runtime-deployment",
    "setup": "npm install && npm run db:generate && npm run db:push"
  }
}
